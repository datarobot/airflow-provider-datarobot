# pipelineController configuration
buildDraftPRs: true

repositoryTasks:
  pr:
    phrase:
    change:
      - changedFilesRegex: '.*'
        script:
          - taskName: UnitTests
            definition: jenkins/unit_test.groovy
            environment:
              DEPLOY_ENV: UnitTests
            phase: 1
          - taskName: BuildAndPublish
            definition: jenkins/build_and_publish_testpypi.groovy
            environment:
              DEPLOY_ENV: BuildAndPublish
            phase: 2